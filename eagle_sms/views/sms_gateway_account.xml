<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="eagle_sms_gateway_account_form" model="ir.ui.view">
        <field name="name">eagle.sms.gateway.accountform</field>
        <field name="model">sms.gateway.account</field>
        <field name="arch" type="xml">
            <form string="">
                <sheet>
                    <group>
                        <group>
                            <field name="name" />
                            <field name="gateway_id"/>
                        </group>
                        <group>
                            <field name="user_string" nolabel="1"/>
                            <field name="user_id" nolabel="1"/>
                            <field name="password_string" nolabel="1"/>
                            <field name="password" nolabel="1"/>

                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <record id="sms_gateway_account_form_action" model="ir.actions.act_window">
        <field name="name">sms.gateway.account.form.action</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">sms.gateway.account</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
          <p class="oe_view_nocontent_create">
            <!-- Add Text Here -->
          </p><p>
            <!-- More details about what a user can do with this object will be OK -->
          </p>
        </field>
    </record>
    <!-- This Menu Item must have a parent and an action -->
    <menuitem id="eagle_sms_gateway_account_menu" name="SMS Gateway Account" parent="eagle_sms_root" action="sms_gateway_account_form_action" sequence="6"/>
</odoo>
